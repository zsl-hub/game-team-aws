services:
  app-backend:
    build: ./backend
    ports:
      - ${BACKEND_PORT}:${BACKEND_PORT}

  app-frontend:
    build: ./frontend
    ports:
      - ${FRONTEND_PORT}:${FRONTEND_PORT}
    depends_on:
      - "app-backend"